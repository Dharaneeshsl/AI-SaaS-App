{
  "name": "ai-saas-app",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack AI SaaS platform — React frontend + Node.js backend. One command to run everything.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend/AI-SASS install",
    "build": "npm --prefix frontend/AI-SASS run build",
    "start": "node scripts/start.cjs",
    "verify": "npm --prefix backend run lint && npm --prefix backend test && npm --prefix frontend/AI-SASS run lint && npm --prefix frontend/AI-SASS run build"
  },
  "engines": {
    "node": ">=20"
  }
}
